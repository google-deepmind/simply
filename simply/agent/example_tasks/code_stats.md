Go through all the python files in the current folder (recursively, with depth
limit 2), figure out all the classes defined. Then for each class, count the
number of times it is used in this codebase. Finally, generate a report of the
classes, sorted by reverse order of the number of usages.
